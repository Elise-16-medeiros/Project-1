export default function Hero() {
    return (
      <section
        style={{
          backgroundImage: `url(${yourImageUrl})`,
          backgroundSize: "cover",
        }}
      >
        Hero
      </section>
    );
}
